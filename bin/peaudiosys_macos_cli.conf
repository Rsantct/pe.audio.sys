# Remote host (or IP)
remote = my_dsp_host.local

# JackTrip settings:
# Bit resolution depends on the remote JackTrip HUB server
# Queue Buffer Length, in Packet Size (default 4)
queue = 4
# Packet Redundancy to avoid glitches with packet losses (default 1)
redundancy = 1
